import React, { useEffect } from 'react';

const Annotations = ({ onBack }) => {
  return (
    <section id="annotations">
      <h2>Annotations</h2>
      <p>Details about the client.</p>
    </section>
  );
};

export default Annotations;